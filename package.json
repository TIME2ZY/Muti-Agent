{
  "name": "multi-agent-invoke-ui",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server/index.js",
    "test": "node --test tests/*.js tests/**/*.js",
    "check": "node scripts/check-syntax.js"
  }
}
